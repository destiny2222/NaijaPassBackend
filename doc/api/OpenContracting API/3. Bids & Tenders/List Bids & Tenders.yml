info:
  name: List Bids & Tenders
  type: http
  seq: 4

http:
  method: GET
  url: "{{base_url}}/api/bids?categoryId=1&status=active&search=Lagos"
  params:
    - name: categoryId
      value: "1"
      type: query
    - name: status
      value: active
      type: query
    - name: search
      value: Lagos
      type: query

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
