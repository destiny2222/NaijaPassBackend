info:
  name: Get My KYC Onboarding Status
  type: http
  seq: 6

http:
  method: GET
  url: "{{base_url}}/api/kyc/my"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
