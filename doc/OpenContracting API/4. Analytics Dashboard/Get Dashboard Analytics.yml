info:
  name: Get Dashboard Analytics
  type: http
  seq: 1

http:
  method: GET
  url: "{{base_url}}/api/analytics"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
