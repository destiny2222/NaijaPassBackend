info:
  name: Mock Google Login Redirect
  type: http
  seq: 5

http:
  method: GET
  url: "{{base_url}}/api/auth/google"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
