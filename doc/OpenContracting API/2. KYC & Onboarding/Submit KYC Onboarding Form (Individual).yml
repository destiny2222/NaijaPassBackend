info:
  name: Submit KYC Onboarding Form (Individual)
  type: http
  seq: 4

http:
  method: POST
  url: "{{base_url}}/api/kyc/submit"
  body:
    type: json
    data: |-
      {
        "type": "individual",
        "email": "john.doe@example.com",
        "phoneNumber": "+2348087654321"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
